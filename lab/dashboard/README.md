# Dashboard v0 (Next.js, Arabic RTL)

Review queue with preview and editing, calendar, funnel with preorder counter, job status.
Talks to the lab API (`uv run uvicorn lab.api:app --port 8000`).

```bash
cp .env.local.example .env.local
npm install
npm run dev        # http://localhost:3000
```
